<!-- res/drawable/border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 내부 색상 설정 -->
    <stroke
        android:width="2dp"
        android:color="@android:color/darker_gray" /> <!-- 테두리 색상과 두께 설정 -->
    <corners android:radius="4dp"/> <!-- 모서리 둥글기 설정 -->
</shape>
